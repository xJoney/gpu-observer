global:
  resolve_timeout: 5m

route:
  group_by: ['alertname', 'machine']
  group_wait: 10s
  group_interval: 10s
  repeat_interval: 1h
  receiver: 'console'

receivers:
  - name: 'console'
    webhook_configs:
      - url: 'http://172.17.0.1:8081/alert'
        send_resolved: true